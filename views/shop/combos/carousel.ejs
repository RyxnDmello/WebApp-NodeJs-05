<% combos.products.forEach((product) => { %>
<form class="combos-slide" action="/shop/product" method="post">
  <input type="hidden" name="ID" value="<%= product.ID %>" />
  <input type="hidden" name="brand" value="<%= combos.post.brand %>" />
  <input type="hidden" name="type" value="<%= combos.post.type %>" />
  <input type="hidden" name="class" value="<%= combos.post.class %>" />

  <div class="combos-slide-images">
    <% product.images.forEach((image) => { %>
    <img class="combos-slide-image" src="<%= image %>" />
    <% }) %>
  </div>

  <div class="combos-slide-information">
    <div class="combos-slide-colors">
      <% product.colors.forEach((color) => { %>
      <h4 class="combos-slide-color"><%= color %></h4>
      <% }) %>
    </div>

    <div class="combos-slide-details">
      <div class="combos-slide-sponsor">Sony X CyberVerse</div>
      <div class="combos-slide-price">$<%= product.price %></div>
    </div>

    <div class="combos-slide-buttons">
      <button
        class="combos-slide-button"
        type="submit"
        name="action"
        value="CART"
      >
        <img
          class="combos-slide-button-image"
          src="/buttons/shop/cart-add.png"
        />

        <p class="combos-slide-button-text">Add To Cart</p>
      </button>

      <button
        class="combos-slide-button"
        value="WISHLIST"
        type="submit"
        name="action"
      >
        <img
          class="combos-slide-button-image"
          src="/buttons/shop/wish-add.png"
        />
        <p class="combos-slide-button-text">Wishlist</p>
      </button>
    </div>
  </div>
</form>
<% }) %>
